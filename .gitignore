# environments
.venv/
venv/
__pycache__/
.pytest_cache/

# database & runtime data
data/

# image corpora (labels live in the DB, files stay local)
dislike/
found/
input/
liked_images/
matches/
maybes/

# legacy v1 model artifact (now under legacy/)
legacy/best.pt

# local Claude Code settings
.claude/settings.local.json

# dataset export snapshots
exports/
