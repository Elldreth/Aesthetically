fastapi>=0.115
uvicorn[standard]>=0.30
pillow>=10.0
httpx>=0.27

# ML stack (Phase 2+)
# torch: install the CUDA wheel first for GPU embedding:
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
torch>=2.6
transformers>=4.49
scikit-learn>=1.4
numpy>=1.26
imagehash>=4.3
# anime hand detection for the hand-quality score (CPU inference); needs the
# anime YOLO seg model at ANIME_HAND_MODEL (defaults to the Artifex models dir).
ultralytics>=8.3

# dev
pytest>=8.0
